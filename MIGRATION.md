# SALTMDB Database Migration Log & Guide

This document tracks schema modifications across alpha versions and provides instructions and SQL statements for migrating production databases.

---

## Version Schema Registry

| Package Version | Schema Version | Modifications | Migration Action |
| :--- | :---: | :--- | :--- |
| `v0.1.0-alpha.6` | 1 | Initial FTS5 virtual tables and events schema | Baseline schema creation |
| `v0.1.0-alpha.7` | 2 | Added temporal SCD columns (`valid_from`, `valid_to`) to `entities`; added `relations` edge table | Column migrations on `entities`; DDL creation of `relations` |
| `v0.1.0-alpha.8` | 2 | No schema changes. Updated Python tool signatures, deduplication logic, tag normalization, and event-read tools | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.9` | 2 | No schema changes. Added Relations Topology graph visualizer and modal click-through links to database viewer | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.10` | 2 | No schema changes. Fixed string escaping syntax error on Outfit font-family definition in server JS block | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.11` | 2 | No schema changes. Added stop_db_viewer MCP tool for programmatic viewer lifecycle control | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.12` | 2 | No schema changes. Fixed path leak in MIGRATION.md and build_review_report.py by resolving paths dynamically | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.13` | 2 | No schema changes. Mocked viewer subprocess inside unit tests to prevent test environment variable pollution | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.14` | 2 | No schema changes. Added archive_memory MCP tool and implemented dynamic pending/resolved event status mapping | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.15` | 2 | No schema changes. Redirected database viewer background startup logs to viewer.log and added startup integrity check | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.16` | 3 | Added `metadata` TEXT column to `entities` for JSON structured filtering; added safe FTS5 query parser fallback, search explain mode, orphan detection, and duplicate checking | **Column Migration on `entities`** (handled automatically or manually via ALTER TABLE) |
| `v0.1.0-alpha.17` | 3 | No schema changes. Capped default get_recent_events limit to 20 and truncated large operational log payloads | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.18` | 3 | No schema changes. Added self-healing zombie port clearance check to start_db_viewer | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.19` | 3 | No schema changes. Shipped specialized Google Antigravity Skills for memory ingestion, consolidation, lifecycle, and relations | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.20` | 3 | No schema changes. Renamed skills folders to include prefix to prevent name collisions in global CLI skills paths | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.21` | 4 | Configured FTS5 Porter tokenizer, added search_aliases metadata column; migrated project_id to entities and session_id to events; renamed store_knowledge to store_memory; changed consolidation to soft-archiving | **Automatic Migration** (init_db runs column changes, Drops/Recreates FTS5 index automatically) |
| `v0.1.0-alpha.22` | 4 | Added E2E test suite; fixed title secret redaction, tag search canonicalization, duplicate check null safety, self-referential relations, bulk consolidation relation re-pointing, viewer CORS OPTIONS preflight, pagination bounds, and connection leaks | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.23` | 4 | Usability refactoring following subagent blind testing; implemented smart UUID/title entity resolution, tool parameter aliasing (query, event_type, text, tag, owner, etc.), init_db default arguments, flexible bulk string archiving, and configurable viewer port | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.24` | 5 | REVIEW_1.md architectural overhaul; added context_id domain-agnostic scoping; demoted owner_id to provenance metadata for shared memories; removed LRU decay entirely; added query stop-words normalization, include_related search, graph_exhausted signal, analyze_lineage tool, and auto-linked consolidated_from lineage edges | **Automatic Migration** (init_db auto-adds context_id columns to entities & events tables) |
| `v0.1.0-alpha.25` | 5 | Viewer & Server comprehensive audit fixes; fixed bulk_commit_consolidation predicate direction to `consolidated_from`, context_id column persistence in store_memory & log_event, start_db_viewer port check; overhaul of database viewer SPA with Stats Dashboard, FTS Search UI, server-side filter dropdowns, Lineage tab, complete entity detail modal, and interactive graph zoom/pan & predicate legend | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.26` | 5 | Fixed critical frontend JavaScript regex syntax crash in `saltmdb_viewer.py` by converting SPA HTML templates to raw string literals (`r"""..."""`); fixed `start_db_viewer` environment and `--port` parameter passing; updated `get_entities` empty string filter logic; updated `get_lineage` entity resolution by ID or title | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.27` | 5 | Codebase refactored from single `saltmdb_server.py` monolith into `src/saltmdb` package layout (`db/`, `domain/services/`, `mcp/`, `viewer/`, `utils/`); entry point changed from `saltmdb_server.py` to `python -m saltmdb` | **No Action Required** (no schema change; update MCP client config to use `python -m saltmdb` instead of `saltmdb_server.py`) |
| `v0.1.0-alpha.28` | 5 | No schema changes. Internal service-layer cleanups and stabilization on refactor branch | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.29` | 6 | Added Hybrid FTS5 + Semantic Vector RRF Search: new `sqlite-vec` `entity_embeddings` vec0 virtual table; `embedding_status` column on `entities`; `embedding_service.py` for lazy `fastembed` ONNX inference; parallel `ThreadPoolExecutor` search with RRF merge; `SALTMDB_ENABLE_SEMANTIC` env-var flag (read-path only) | **Automatic Migration** (`init_db` auto-adds `embedding_status` column and creates `entity_embeddings` virtual table; run `scratch/backfill_embeddings.py` once for pre-existing rows) |
| `v0.1.0-alpha.30` | 6 | Major audit release: added 11 SQL performance indexes; bounded `ThreadPoolExecutor` for background embeddings; security hardening (CORS restricted to localhost, raw exception leakage suppressed, redaction fast-path bypass fix); viewer SPA enhancements (server-side tag filtering, dedicated `/api/relations/graph` endpoint, PID tracking for clean stop); MCP API standardization (`commit_consolidation` `owner_id`/`context_id` params, `get_canonical_tags` `query` rename); complete documentation sync across README, INSTALL, AGENT_GUIDE, and pyproject.toml | **Automatic Migration** (`init_db` creates secondary SQL indexes automatically) |
| `v0.1.0-alpha.31` | 6 | Hotfix release: fixed thread-safety race condition in hybrid search by giving `semantic_search` its own dedicated SQLite connection; fixed `embedding_status` showing as `pending` for archived memories in web UI/API (added SCD2 history status copying, viewer detail API field fix, and automatic DB migration/trigger) | **Automatic Migration** (`init_db` auto-backfills `embedding_status = 'archived'` for existing archived memories) |
| `v0.1.0-alpha.32` | 6 | Production release: enabled Hybrid FTS5 + Dense Vector RRF search by default across all clients (`SALTMDB_ENABLE_SEMANTIC` defaults to `true`); fixed `store_memory` background embedding parameter resolution (`target_db`); fixed `sqlite-vec` virtual table upsert syntax (`DELETE` + `INSERT`); added automatic server startup embedding backfill queue; preserved score calculation precision (6 decimal places); verified 100% blind retrieval accuracy in multi-agent stress benchmark | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.33` | 6 | Production release: fixed MCP tool parameter alias resolution (`query`, `q`, `keywords`) in `search_memory` wrapper; made `include_related=True` default in `search_memory` so agents automatically receive 1-hop knowledge graph relations without explicit parameters; expanded test suite to 22 unit, integration, and E2E tests | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.34` | 6 | UI/UX Overhaul release: complete redesign of SALTMDB Database Viewer SPA with 2026 Dark Glassmorphism aesthetics, 6-card Bento Grid dashboard (executive metrics, SVG donut charts, embedding pipeline health, live event ticker stream), live Dense Vector & Hybrid RRF Search Playground, SVG Knowledge Graph topology network explorer, `/api/embeddings/backfill` (POST) trigger endpoint, and `Ctrl+K` command palette & `1`–`8` hotkeys | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.35` | 6 | Graph Density & Physics release: added Graph Density Control Toolbar to Database Viewer (Active Memories filter defaulting to hide archived snapshots; Degree filter defaulting to Connected nodes; Predicate filter; Live node title search highlight); upgraded graph renderer with degree-based dynamic node radii, iterative force simulation clustering physics, and 1-hop hover connection highlighting | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.36` | 6 | Dynamic Predicates release: updated Graph Topology view in Database Viewer to extract all custom relation predicates directly from database graph data at runtime, rendering dynamic color-coded badge legends and dynamic filter dropdown options for all unique domain predicates | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.37` | 6 | Metrics Accuracy release: updated Database Viewer Bento Grid overview stat card to calculate Vector Embeddings Ready % strictly against `active_entities` (`raw` + `consolidated`) instead of `total_entities` (which included archived snapshots), adding `active_entities` field to `/api/stats` and clear description label (`X / Y active memories indexed`) | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.38` | 6 | Memory Detail Modal Expansion release: upgraded Database Viewer memory detail drawer (`openEntityDetail`) with a complete Metadata Grid (Created At, Updated At, Last Accessed, Entity UUID, Context ID, Weight, Is Core #core flag), custom JSON metadata inspector, and interactive Knowledge Graph relation list (outgoing & incoming links with clickable targets) | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.39` | 6 | UI Fixes release: fixed native `<select>` dropdown styling (`select` & `select option` background/color rules for dark mode); implemented `loadLineage()` JS handler and enhanced `get_lineage` backend query with ID prefix / partial title matching so searching entity IDs or titles in Lineage Inspector renders the multi-generation ancestry tree | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.40` | 7 | Mechanical Text Quality Gate & Consolidation Deduplication release: added pre-embedding quality evaluation (`evaluate_memory_quality`), Shannon character entropy ($H(X)$), TTR lexical diversity, fluff regex filters, and Tier 4 technical specificity scoring; added sub-ms SHA-256 exact hash collision lookup (`compute_content_hash`); extended quality gate and target exclusion (`exclude_ids`) to `commit_consolidation`; added `content_hash`, `quality_score`, `quality_status`, and `quality_flags` columns on `entities` table | **Automatic Migration** (`init_db` auto-adds `content_hash`, `quality_score`, `quality_status`, `quality_flags` columns and `idx_entities_content_hash` index) |
| `v0.1.0-alpha.41` | 7 | Word N-Gram Repetition Scanner & Markdown Quality release: added 3-gram ($> 0.30$) and 5-gram ($> 0.20$) duplicate sequence scanners catching character entropy bypass loops; raised TTR threshold to $< 0.35$; added deterministic Markdown syntax integrity checks (unclosed ``` fences and table pipe symmetry), MSDI density scoring, untyped code block penalties, and non-hierarchical header progression checks | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.42` | 7 | DB Viewer Responsiveness & JS Fix release: fixed `Uncaught ReferenceError: handleEntitySearch is not defined` in frontend templates by defining debounced search handler; upgraded `SALTMDBTCPServer` to inherit from `socketserver.ThreadingMixIn` with `daemon_threads = True` to enable concurrent HTTP request handling and eliminate browser freezes during heavy API queries | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.43` | 7 | Advanced Quality Metrics, KenLM Perplexity & Calibrated Supersession release: added zero-dependency pure Python Bigram Transition Perplexity Gate (`perplexity.py`) rejecting nonsensical word salad; added prose extraction (`extract_prose_content`) protecting code/paths from false readability rejections; added Coleman-Liau Readability Index ($\text{CLI}$); wired idempotent auto-formatting ($f(f(x)) = f(x)$); added calibrated auto-supersession ($\ge 0.88$ similarity under matching owner); upgraded Librarian with quality-weighted LRU decay while exempting core (`is_core = 1`) and pinned (`metadata.is_pinned = 1`) directives | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.44` | 7 | DB Viewer Pagination Release: added pagination control bars (item counters `Showing X–Y of Z`, page indicators `Page X of Y`, and Previous/Next navigation buttons) to Knowledge Entities (`view-entities`) and Operational Event Ledger (`view-events`) views; added event type and agent search filters to Event Ledger | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.45` | 7 | Package release: documentation alignment, pyproject.toml version sync, core flag persistence fix, and owner_id standardization | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.46` | 7 | MCP Tool Consolidation Release: refactored MCP tool palette from 23 to 10 consolidated tools; added polymorphic string/list handling for `archive_memory`, `manage_relation`, and `commit_consolidation`; unified graph inspection into `inspect_graph`; unified events into `get_events`; unified volatile secret management into `ephemeral_memory`; moved viewer/snapshot management to CLI | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.47` | 7 | Auto-Supersession & Duplicate Check Fix Release: upgraded candidate decision metric to ONNX embedding cosine distance; replaced unconfirmed write-time weight demotion and auto-linking with reviewable `supersession_candidate` event logging; enabled store-and-warn duplicate write handling with structured warning returns | **No Action Required** (fully backward-compatible) |
| `v0.1.0-alpha.48` | 7 | No new columns. Fixed a cross-owner shared-scope dedup blind spot in `check_duplicate_memories` (scope='shared' memories from other owners are now correctly surfaced as candidates); retired `project_id` in favor of `context_id` (both columns already existed since alpha.21) with an automatic backfill; centralized dedup/BM25/quality-gate magic numbers into `config.py`; consolidated the MCP tool `kwargs`-alias-resolution boilerplate across all 10 tools into shared helpers; removed the dead duplicate re-export surface from `saltmdb_server.py` | **Automatic Migration** (init_db backfills `context_id` from `project_id`). **Signature change**: `project_id` param removed from `memory_service.store_memory`/`search_memory`/`check_duplicate_memories` — still accepted as an input alias (`project_id`/`project`) at the MCP tool layer, so MCP callers are unaffected; direct importers of the domain service functions must switch to `context_id` |
| `v0.1.0-alpha.49` | 7 | Documentation-only release. Added a documentation-sync checklist to `CONTRIBUTING.md` (§4) covering `MIGRATION.md` entries, `AGENT_GUIDE.md` tool-signature accuracy, and repo-vs-bundled-skill-copy drift; added a "Self-Update Protocol" (`AGENT_GUIDE.md` §6) instructing agents to reconcile their own global instructions and bundled skill files when told SALTMDB was updated, rather than only reading the guide passively; synced the repo's stale `MULTI_AGENT_ORCHESTRATION.md` (an old 13-commandment Antigravity-CLI draft) to the already-current Claude-Code-specific version | **No Action Required** (fully backward-compatible, no code or schema changes) |
| `v0.1.0-alpha.50` | 7 | Documentation-only release. Ran the AGENT_GUIDE.md §6 Self-Update Protocol and found the alpha.49 drift fix was incomplete: only `MULTI_AGENT_ORCHESTRATION.md` had been synced, while the repo root's `ORCHESTRATOR.md` and `WORKER_TEMPLATE.md` were still stale (generic `agent_orchestrator` owner ID, unfilled `[PASTE FULL 14 RULES HERE]` placeholder, generic `send_message` tool reference) against the already-current, Claude-Code-specific bundled skill copies (`~/.claude/skills/saltmdb-subagent-orchestration/references/`). Synced both repo root files to match the skill copies | **No Action Required** (fully backward-compatible, no code or schema changes) |
| `v0.1.0-alpha.51` | 7 | Documentation correction. The alpha.50 sync (and the alpha.49 `MULTI_AGENT_ORCHESTRATION.md` sync before it) copied Claude-Code-specific wording (`owner_id="claude"`, the `Agent` tool, `SendMessage(to="main", ...)`, `~/.claude/CLAUDE.md`) into the repo-root orchestration docs. Since SALTMDB is a cross-agent, multi-vendor project, that direction was backwards: the repo root should stay a vendor-neutral reference template, while the bundled skill copies (`~/.claude/skills/saltmdb-subagent-orchestration/references/`) are the ones allowed to be Claude-Code-concrete. Reverted `ORCHESTRATOR.md` and `WORKER_TEMPLATE.md` to their vendor-neutral form and rewrote `MULTI_AGENT_ORCHESTRATION.md` as a fresh vendor-neutral version (not a revert to its old, also-vendor-biased 13-commandment Antigravity draft). Clarified the intended repo-root-vs-skill-bundle relationship in `AGENT_GUIDE.md` §6 and `CONTRIBUTING.md` §4 so this doesn't flip direction again | **No Action Required** (fully backward-compatible, no code or schema changes) |
| `v0.1.0-alpha.52` | 7 | Bugfix release. `commit_consolidation` (`relation_service.py`) never submitted a background embedding job for the newly created `'consolidated'` entity, unlike `store_memory` which does so immediately after every raw memory write. Consolidated entities correctly defaulted `embedding_status` to `'pending'` but then sat un-embedded until a manual `/api/embeddings/backfill` click or the next server restart's startup backfill — also making them invisible to `librarian_service.scout_consolidated_supersessions` (which requires `embedding_status = 'ready'`) in the meantime. Fixed by submitting the same `embedding_service.embed_entity_async` background job at the end of `commit_consolidation`, deriving the target DB file via `PRAGMA database_list` on the live connection (rather than `db_path or get_db_path()`) so it resolves correctly regardless of whether the caller passed `db_path`, `db_connection`, or neither — `bulk_commit_consolidation` and the test suite both call `commit_consolidation` with only `db_connection`, which would otherwise misfire against the default DB path. Added `tests/test_consolidation_embedding_trigger.py` covering this | **No Action Required** (fully backward-compatible, no schema change) |
| `v0.1.0-alpha.53` | 7 | SQLite concurrency hardening release. No schema changes. Switched write transactions from Python sqlite3's implicit deferred `BEGIN` to an explicit `BEGIN IMMEDIATE` (`write_transaction`/`write_transaction_retrying` in `connection.py`, rolled out to every write call site in `schema.py`, `locks.py`, `event_service.py`, `memory_service.py`, `relation_service.py`, `librarian_service.py`) so `PRAGMA busy_timeout` is actually honored on write-write contention instead of being silently bypassed by the deferred-transaction's read-to-write lock upgrade; added bounded jittered retry/backoff on "database is locked"; added `PRAGMA optimize` plus WAL-checkpoint-state logging on every connection close (`close_connection`); gave the Librarian a dedicated `PRAGMA wal_checkpoint(TRUNCATE)` + `PRAGMA optimize=0x10002` maintenance duty run once per invocation while the leader lock is held; redirected the Librarian subprocess's stdout/stderr from `DEVNULL` to an append-mode `librarian.log` (matching the existing `viewer.log` precedent) for debuggability; wired the previously-unused `LIBRARIAN_LOCK_STALE_MINUTES` and `LIBRARIAN_TRIGGER_COOLDOWN_S` config constants into `locks.py`/`librarian_service.py` in place of hardcoded literals. **User-visible behavior change**: `bulk_archive_memory`, `bulk_commit_consolidation`, and `bulk_store_relations` are now genuinely atomic (all-or-nothing) — each batch runs inside a single write transaction and a failure anywhere in the loop rolls back every item in that batch, returning one top-level error result instead of a mixed per-item success/error list. Callers that previously relied on partial-success semantics from these three bulk tools will now see the whole batch fail together instead of a partial commit. *(Note: at the time of this alpha.53 release, `config.py`'s `__version__` string was still `"0.1.0-alpha.47"`, out of sync with this table's actual last-released row — flagged here for whoever owns the next version bump rather than silently corrected, since `config.py` was out of scope for this change. **Resolved**: `config.py` now reads `__version__ = "0.1.0-alpha.60"`, in sync with `pyproject.toml` and this table.)* | **No Action Required** (fully backward-compatible, no schema change) |
| `v0.1.0-alpha.54` | 7 | Tag resolution unification release. No schema changes. Unified the two divergent tag-write paths (`store_memory` and `commit_consolidation`) into one shared `resolve_or_create_tag()` helper (added alongside a `normalize_tag_name()` syntactic helper) in `memory_service.py`. **Behavior-relevant fix**: `commit_consolidation`'s (`relation_service.py`) tag-write path previously did an exact-name match only, completely ignoring `canonical_id` and never populating `normalized_name` on new tag rows it created — meaning a consolidated entity tagged with an already-aliased tag name (e.g. one merged via `merge_tags`) would silently bypass the alias and get tagged with a non-canonical duplicate instead of following the existing merge. Consolidated entities' tags now correctly resolve through `canonical_id` chains like every other write path. Also added a deterministic plural/suffix tag-matching fallback inside `resolve_or_create_tag()` (catches e.g. `#skill`/`#skills` coexisting without needing embeddings), and soft write-time tag shape validation (sanitizes malformed tag strings and logs an `issue` event, never hard-rejects). Read-path tag-normalization duplication was also cleaned up: `search_memory()`'s `tags_filter` normalization (both the main filter block and the `explain_mode` branch) and `librarian_service._resolve_tag_id()` now call the shared `normalize_tag_name()` instead of re-implementing the same one-line `#`-prefix check. | **No Action Required** (fully backward-compatible, no schema change) |
| `v0.1.0-alpha.55` | 8 | Relation System Quick Wins release. **Schema change**: added a `predicates` table (`id`, `name` UNIQUE, `normalized_name`, `canonical_id` self-FK ON DELETE SET NULL, `created_at`) mirroring tags' `canonical_id`-alias shape (a standalone table, not a literal reuse of `tags` — that machinery turned out to be tag-specific, not generic, when explored: no `kind` column, `#`-prefix-assuming sanitization logic), seeded with a starter canonical predicate vocabulary (`resolves`, `depends_on`, `references`, `elaborates_on`, `consolidated_from`, `supersedes`, `relates_to`) with `relates_to`/`references` pre-aliased onto `elaborates_on` (observed real-world drift where all three were used interchangeably); added a `UNIQUE(source_id, target_id, predicate)` index on `relations` (`idx_relations_unique_edge`), preceded by a one-time dedup backfill collapsing any pre-existing duplicate edges to the earliest-inserted row (by `rowid`, not `id`, since `id` is a random UUID unrelated to insertion order). **Behavior change**: `store_relation`/`bulk_store_relations` now canonicalize the `predicate` string at write time via new `resolve_or_create_predicate()` in `relation_service.py` (non-blocking — an unrecognized predicate is always auto-created, never rejected) and store the CANONICALIZED string in `relations.predicate`, not the raw input; a duplicate `(source_id, target_id, predicate)` write now no-ops (`ON CONFLICT DO NOTHING`, returns the existing row's ID with an "already exists" message) instead of inserting a second identical edge. Added `get_canonical_predicates(query)` MCP tool mirroring `get_canonical_tags`. `commit_consolidation`'s direct `'consolidated_from'` edge insert is intentionally NOT routed through `resolve_or_create_predicate` this round (out of scope — already a seeded canonical predicate). Deferred to a future round: Graphiti-style `valid_to` bitemporal versioning of relation edges, `parent_ids`/`relations` redundancy resolution, confidence/provenance fields. 125/125 tests passing (106 baseline + 19 new, `tests/test_relation_service.py`). | **Automatic Migration** (`init_db` auto-creates the `predicates` table + seed rows and the new unique index; the dedup backfill runs automatically first) |
| `v0.1.0-alpha.57` | 10 | Relation System Medium-Term release. **Schema change**: `idx_relations_unique_edge` on `relations` (`source_id`, `target_id`, `predicate`) is now a PARTIAL unique index (`WHERE valid_to IS NULL`), replacing the plain unique index added in alpha.55 — required so an expired edge and its live replacement can legitimately coexist for the same tuple. Since `CREATE UNIQUE INDEX IF NOT EXISTS` is a silent no-op against an already-existing same-named index even when its definition differs, `init_db()` now unconditionally `DROP INDEX IF EXISTS` + recreates it every run (idempotent, cheap once already migrated) rather than relying on `IF NOT EXISTS` alone. No data backfill needed — every pre-existing row already has `valid_to IS NULL` since nothing had ever set it. **Behavior change (Graphiti-style invalidation)**: `commit_consolidation` (`relation_service.py`) no longer mutates existing relation rows in place (`UPDATE relations SET source_id/target_id = ...`, which also carried an unguarded duplicate-edge crash risk). It now expires the old edge (`valid_to = now`, row otherwise untouched, preserving what it used to point at) and inserts a new edge at the consolidated entity, deduped via `ON CONFLICT ... WHERE valid_to IS NULL DO NOTHING`, with a self-loop guard for edges directly between two parents merged in the same batch. Edges with `predicate = 'consolidated_from'` are deliberately excluded from this generic repointing (they remain an immutable audit trail written only by the existing per-parent insert loop) — without this exclusion, re-merging a previously-consolidated entity into a later consolidation would corrupt multi-generation lineage by skipping a generation. Invalidation stays consolidation-only this round (no new `invalidate_relation` tool). **Behavior change (`parent_ids`/`relations` redundancy resolved)**: `analyze_lineage` (`relation_service.py`, backing `inspect_graph(mode='lineage')`) no longer walks `entities.parent_ids` via `LIKE` string-matching — it now walks the `relations` table (`predicate = 'consolidated_from'`) via the same recursive-CTE/point-in-time/cycle-guard pattern as `analyze_dependencies`. `parent_ids` is kept (has a confirmed external consumer: the viewer's entity detail view) but is now derived/display-only, not authoritative — documented as such in `schema.py` and `analyze_lineage`'s docstring. Per-ancestor dicts dropped `parent_ids`, gained `owner_id`/`updated_at`. **New capability**: both `analyze_dependencies` and `analyze_lineage` (and `inspect_graph`, aliases `point_in_time`/`as_of`/`at`) accept an optional `point_in_time` parameter to query relation-graph state as of a past moment, filtering on both `valid_to` and a new symmetric `valid_from` lower bound. **Adjacent fixes found during implementation**: `detect_orphaned_memories` and `search_memory`'s `include_related` batch query both gained `valid_to`-awareness (an entity whose only relations are all-expired now correctly counts as an orphan; expired edges no longer leak into search results' `related_entities`) — these were previously-latent no-ops since `valid_to` had never been populated by anything, and would have become live regressions the moment this round's `commit_consolidation` change shipped if left unfixed. The viewer's `get_lineage` (`viewer/routes.py`) — previously its own third, independent, doubly-nested-CTE lineage implementation, redundant with and divergent from the (then `parent_ids`-based) canonical `analyze_lineage` — now delegates to `relation_service.analyze_lineage` directly; its dead `edges`/`ancestry_tree` response fields (confirmed unused by the frontend) were dropped. Also fixed fragile positional `parent_ids` tuple indexing (`r[9]`/`row[9]`, coincidentally aligned only by chance across two differently-shaped SELECT statements) in `get_entities`/`get_entity_detail` to `sqlite3.Row` key access. 169/169 tests passing (142 baseline + 27 new, `tests/test_relation_service.py`, `tests/test_mcp_tools.py`, `tests/test_viewer_routes.py`). | **Automatic Migration** (`init_db` drops and recreates `idx_relations_unique_edge` as a partial index; no data backfill required) |
| `v0.1.0-alpha.56` | 9 | Information Categorization Quick Win release. **Schema change**: added a `memory_type TEXT CHECK(memory_type IN ('fact','event','procedure','decision','preference')) DEFAULT 'fact'` column on `entities`, shaped the same way as the existing `status` column (plain CHECK-constrained TEXT with a SQL-level default, no separate lookup table), plus a partial index `idx_entities_memory_type` (`WHERE status != 'archived'`, mirroring the other partial indexes on this table). This resolves the design collision flagged in `research/README.md`'s cross-cutting findings between Track 5's proposed `type:value` tag-namespace convention and Track 7's proposed dedicated column — decided in favor of the column (queryable/indexable, closed-vocabulary by construction, doesn't recreate the free-text tag-fragmentation surface both tracks warned about). **Behavior change**: `store_memory` (`memory_service.py`) gained an optional `memory_type` param — validated against the fixed 5-value list, `None` on an update preserves the existing DB value, `None` on a brand-new insert defaults to `'fact'` (same "don't touch unless explicit" semantics as `is_core`); `search_memory` gained an optional `memory_type_filter` param, and every result item now echoes a `"memory_type"` key. Both wired through the MCP tool layer (`tools.py`) via the existing `_resolve()`/kwargs-alias pattern (`memory_type`/`type`/`kind` aliases on `store_memory`; `memory_type_filter`/`memory_type`/`type_filter` aliases on `search_memory`). No new discovery tool added — the vocabulary is small, fixed, and closed, so the `Literal[...]` type hints plus docstrings are sufficient documentation. Deferred to a future round: Track 7's Phase 2 (a `domain` column populated via embedding-cluster-assisted UMAP/HDBSCAN suggestions, never silent auto-tagging). `commit_consolidation` was intentionally left unwired this round — consolidated entities simply get the DB default `'fact'`. 142/142 tests passing (125 baseline + 17 new, `tests/test_memory_type.py` + `tests/test_mcp_tools.py` additions). | **Automatic Migration** (`init_db` auto-adds the `memory_type` column + partial index; existing rows backfill to `'fact'` via the column's own SQL `DEFAULT`, no explicit `UPDATE` backfill statement needed) |
| `v0.1.0-alpha.58` | 11 | Information Categorization Phase 2 (manual-scope) release. **Schema change**: added a `domain TEXT` column on `entities` — deliberately shaped differently from `memory_type`: no `CHECK` constraint and no `DEFAULT`, because `domain`'s vocabulary (top-level projects/life-areas) is expected to grow as new projects start, and SQLite cannot `ALTER` a `CHECK` constraint in place (widening it later would require a full table rebuild). Vocabulary enforcement moved to the service layer instead: a new `VALID_DOMAINS` constant in `memory_service.py`, currently `('SALTMDB', 'CADET', 'Business', 'Homelab', 'General')` (mapped onto zbalint's existing Tea/Ops persona-routing split plus the two active coding projects plus a catch-all). Also added a partial index `idx_entities_domain` (`WHERE status != 'archived'`, same shape as `idx_entities_memory_type`). **Behavior change**: `store_memory` gained an optional `domain` param, validated against `VALID_DOMAINS` at the service layer (returns an error string on an invalid value, never raises); `None` on a brand-new insert stores SQL `NULL` (no default — many memories won't have one obvious domain), `None` on an update preserves the existing DB value (`COALESCE(?, entities.domain)`, same "don't touch unless explicit" pattern as `memory_type`/`is_core`). `search_memory` gained an optional `domain_filter` param (`WHERE e.domain = ?`, no validation on the filter side — an unmatched value just yields zero results), and every result item (plus `scan_memories` items) now echoes a possibly-null `"domain"` key. Both wired through the MCP tool layer (`tools.py`) via the existing `_resolve()`/kwargs-alias pattern (`domain`/`area`/`category` aliases on `store_memory`; `domain_filter`/`domain`/`area_filter` aliases on `search_memory`) — picked to avoid colliding with `get_canonical_tags`'s pre-existing, unrelated `domain` param (a tag-name substring filter) that already lived in the same file. While wiring the tool-layer docstrings dynamically from `VALID_DOMAINS`, found and fixed a latent bug: an f-string used as a Python function-body docstring silently compiles to `None` (`__doc__` requires a plain string literal, not an f-string AST node), which would have registered an empty MCP tool description for both `store_memory` and `search_memory` — fixed by moving the dynamic description into `@mcp.tool(description=f"""...""")` instead. **Scope note**: this ships only the manually-set column plus store/search support — it is explicitly NOT the embedding-cluster-assisted UMAP/HDBSCAN domain-*suggestion* pipeline that the alpha.56 entry deferred; that clustering/suggestion design remains unbuilt and still future work. No new ML dependencies, no background job, no new `log_event` type, no viewer changes (consistent with `memory_type`'s precedent of skipping the viewer). `commit_consolidation` was intentionally left unwired this round, same as `memory_type` in alpha.56 — consolidated entities simply inherit no domain (stays `NULL`, since there's no default to fall back to). 186/186 tests passing (169 baseline + 14 new in `tests/test_domain.py` + 3 new in `tests/test_mcp_tools.py`). | **Automatic Migration** (`init_db` auto-adds the `domain` column + partial index; existing rows stay `NULL`, no backfill) |
| `v0.1.0-alpha.59` | 11 | No schema changes. Viewer UI redesign Phase 1 (Track 1 quick wins): removed glassmorphism (`backdrop-filter: blur(...)` + translucent `rgba` fills) from `.sidebar`, `.topbar`, `.bento-card`, `.data-table-container` in favor of an opaque 4-step surface ladder (`--bg-primary/--bg-secondary/--bg-card/--bg-card-hover/--bg-solid`, differentiated by lightness + hairline borders); `.modal-overlay` blur intentionally kept as the one legitimate exception. Removed the two ambient body radial-gradients and the `.bento-card::before` accent-gradient hover sheen (accent color now reserved strictly for CTA/focus-ring/active-nav/link-emphasis). Replaced the SVG donut chart (stale `stroke-dasharray` binding — a real bug, never recalculated by JS) with a horizontal stacked proportion bar, now correctly bound to live `raw_count`/`consolidated_count`/`archived_count` proportions. Added `font-variant-numeric: tabular-nums` to stat values. Added spacing-scale (4/8/12/16/24/32/48/96px) and radius-scale (4/6/8/12/16px) CSS custom properties, retrofit into bento grid gap/card padding/button padding only; broader retrofit deferred. Documented (not fixed) a categorical/semantic color collision where event/predicate badges reuse the status-triad/accent hues — deferred to a future round. A future round may split the 1728-line template into partials or vendor `d3-force`; explicitly NOT part of this release. All changes confined to `src/saltmdb/viewer/templates.py`. | **No Action Required** (fully backward-compatible, no schema changes) |
| `v0.1.0-alpha.60` | 12 | Agent Memory Survey Quick Wins release (`research/03-agent-memory-systems-survey.md`). **Schema change**: added nullable `valid_at`/`invalid_at DATETIME` columns to `relations` — an independent event/world-time axis alongside the existing `valid_from`/`valid_to` system/transaction-time pair (alpha.57), settable directly by the calling agent rather than only by `commit_consolidation`. Seeded three canonical top-level tags (`episodic`, `semantic`, `procedural`) in the `tags` table, mirroring `predicates`' alpha.55 seeding shape — shipped additively alongside the existing `memory_type` column rather than replacing it (accepted redundancy, an explicit user choice). **Behavior change**: `store_relation` (`relation_service.py`) gained an optional `valid_at` param (defaults to the same `now` as `created_at`/`valid_from` if omitted, untouched by duplicate no-op writes); a new `invalidate_relation(source_id, target_id, predicate, invalid_at=None)` sets only `invalid_at` on the currently-live edge (`valid_to IS NULL` match, predicate canonicalized via the existing `resolve_or_create_predicate` before lookup so aliases still match) — idempotent no-op if already invalidated, `valid_to` is never touched (stays consolidation-only). Wired through `manage_relation` (`tools.py`) via a new `invalidate: bool = False` param plus `invalid_at`/`valid_at` kwargs passthrough — no new top-level MCP tool (the server has 12 registered tools both before and after this change; the long-standing "10 consolidated tools" figure from alpha.46 was already stale, having never been updated after `get_canonical_predicates`/`merge_tags` shipped in later rounds — flagged here rather than silently left, no code change needed). `store_memory` (`memory_service.py`) now appends a short one-time nudge to its success message on brand-new (non-update) stores that included `tags`, suggesting the caller consider `manage_relation` to link related concepts — no server-side extraction, stays LLM-free per design. **Scope note**: a fourth proposed item (forcing `is_core=True` to coerce `scope='shared'`) was planned but dropped before implementation — it collides with `WORKER_TEMPLATE.md`'s documented "Private Core Rule" (subagent workers are required to store their own domain-specific `#core` standing rules with `scope='private'`), which is a real, load-bearing multi-agent isolation convention this round chose not to silently break. Revisit later with a design that doesn't collide (e.g. a separate flag distinguishing cross-agent-pinned facts from per-worker private standing rules). **Deferred**: wiring `valid_at`/`invalid_at` into `point_in_time` traversal (`analyze_dependencies`/`analyze_lineage`/`inspect_graph` stay on `valid_from`/`valid_to` exactly as alpha.57 built them) and automatic semantic-conflict auto-invalidation remain future work. 205/205 tests passing (186 baseline + 19 new, `tests/test_bitemporal_relations.py` + `tests/test_mcp_tools.py` additions). | **Automatic Migration** (`init_db` auto-adds `relations.valid_at`/`invalid_at` columns and seeds the 3 canonical tags; both idempotent, no backfill needed) |

---


## DDL Migrations (v0.1.0-alpha.6 ➔ v0.1.0-alpha.7)

If you are upgrading an existing production `saltmdb.db` database from `v0.1.0-alpha.6` manually (rather than allowing `init_db` to run migrations automatically), run the following SQL statements:

```sql
-- 1. Enable temporal columns on entities table
ALTER TABLE entities ADD COLUMN valid_from TEXT;
ALTER TABLE entities ADD COLUMN valid_to TEXT;

-- 2. Create the typed relationship edges table
CREATE TABLE IF NOT EXISTS relations (
    id TEXT PRIMARY KEY,
    source_id TEXT NOT NULL,
    target_id TEXT NOT NULL,
    predicate TEXT NOT NULL,
    created_at TEXT NOT NULL,
    valid_from TEXT,
    valid_to TEXT,
    FOREIGN KEY (source_id) REFERENCES entities(id) ON DELETE CASCADE,
    FOREIGN KEY (target_id) REFERENCES entities(id) ON DELETE CASCADE
);

-- 3. Create index for relation lookups to accelerate CTE recursive traversals
CREATE INDEX IF NOT EXISTS idx_relations_source_target ON relations (source_id, target_id);
```

---

## DDL Migrations (v0.1.0-alpha.15 ➔ v0.1.0-alpha.16)

If you are upgrading an existing production `saltmdb.db` database from `v0.1.0-alpha.15` manually, run the following SQL statement:

```sql
-- Add metadata JSON column to entities
ALTER TABLE entities ADD COLUMN metadata TEXT;
```

---

## DDL Migrations (v0.1.0-alpha.28 ➔ v0.1.0-alpha.29)

If you are upgrading an existing production `saltmdb.db` manually (rather than allowing `init_db` to run migrations automatically), run the following steps:

```sql
-- 1. Add embedding_status tracking column
ALTER TABLE entities ADD COLUMN embedding_status TEXT DEFAULT 'pending';

-- 2. Mark existing archived memories as 'archived' in embedding_status
UPDATE entities SET embedding_status = 'archived' WHERE status = 'archived';
```

> [!IMPORTANT]
> **Order of Operations for Backfill:**
> Before running `backfill_embeddings.py`, the `entity_embeddings` virtual `vec0` table **must** be created. 
> 
> You can create it by either:
> 1. Launching the server once (`python -m saltmdb`), or
> 2. Running `python -c "from saltmdb.db.schema import init_db; init_db()"`
> 
> If `backfill_embeddings.py` is executed before initializing `init_db()`, embedding generation will fail with `no such table: entity_embeddings` and mark entity statuses as `failed`. If this happens, run `init_db()`, reset failed records (`UPDATE entities SET embedding_status = 'pending' WHERE embedding_status = 'failed'`), and re-run `backfill_embeddings.py`.

Finally, run the one-time backfill script to generate embeddings for existing rows:
```bash
python scratch/backfill_embeddings.py
```

---

To verify your database schema compatibility, run the test suite:

```bash
python -m unittest discover tests
```
If all tests execute and pass cleanly, your database schema is correctly aligned.

---

## DDL Migrations (v0.1.0-alpha.39 ➔ v0.1.0-alpha.40)

If upgrading a production `saltmdb.db` database manually:

```sql
-- 1. Add quality gate metadata columns to entities
ALTER TABLE entities ADD COLUMN content_hash TEXT;
ALTER TABLE entities ADD COLUMN quality_score REAL;
ALTER TABLE entities ADD COLUMN quality_status TEXT;
ALTER TABLE entities ADD COLUMN quality_flags TEXT;

-- 2. Create index for SHA-256 exact deduplication queries
CREATE INDEX IF NOT EXISTS idx_entities_content_hash ON entities(owner_id, content_hash) WHERE status != 'archived';
```

