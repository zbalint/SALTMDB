{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "~/.claude/hooks/saltmdb-session-bootstrap.sh",
            "timeout": 15
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "~/.claude/hooks/saltmdb-pre-action-gate.sh",
            "timeout": 15
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "agent",
            "prompt": "You are the SALTMDB pre-compaction sweep hook. The current session's conversation transcript is about to be compacted and working context will be lost. Review the conversation so far for: unresolved decisions, root-cause fixes to bugs/issues, new architectural rules, or user preferences established in this session but NOT yet persisted to SALTMDB. For each item found, first call mcp__saltmdb__store_memory with check_duplicates_only=true to confirm it isn't already recorded, and if genuinely new, call mcp__saltmdb__store_memory (owner_id='agent_hook_precompact') to persist it, and mcp__saltmdb__log_event for any issue/fix worth a short-term event log entry. Do not report back conversationally -- this is a background sweep.",
            "timeout": 60
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "~/.claude/hooks/saltmdb-self-critique-gate.sh",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
